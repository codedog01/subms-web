<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--springmvc配置文件， 声明controller和其它web相关的对象-->
    <context:component-scan base-package="com.lengao.controller"/>

    <!--视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <mvc:annotation-driven/>
    <!--
      1. 响应ajax请求，返回json
      2. 解决静态资源访问问题。
    -->

    <!--声明拦截器，拦截器可以有0个或多个-->
    <mvc:interceptors>
        <!--声明第一个拦截器-->
        <mvc:interceptor>
            <!--指定拦截器的请求路径-->
            <mvc:mapping path="/user/**"/>
            <bean class="com.lengao.handler.MyInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
    <!--解决中央调度器地址修改为/导致静态资源无法访问-->
    <!--方法一-->
    <!--依赖于服务器的defaultServlet，将请求转发给服务器默认的defaultServlet-->
<!--    <mvc:default-servlet-handler/>-->
    <!--方法二-->
    <!--配置哪些资源不会被前端控制器拦截，创建控制器resourceHttpRequestHandler控制器对象处理-->
    <!--mapping：访问路径url中以images开头的资源，如/images/1.jpg-->
    <!--location：符合mapping的资源去location路径下去找-->
<!--
    <mvc:resources mapping="/images/**" location="/images/"/>
-->

</beans>